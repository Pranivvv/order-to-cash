<core:FragmentDefinition
  xmlns="sap.m"
  xmlns:core="sap.ui.core"
  xmlns:f="sap.ui.layout.form">
  <Dialog
    id="createOrderDialog"
    title="Create Sales Order"
    contentWidth="600px"
    draggable="true"
    resizable="true">
    <content>
      <f:SimpleForm editable="true" layout="ColumnLayout">
        <f:content>
          <Label text="Customer" required="true" />
          <Select
            id="customerSelect"
            selectedKey="{dialog>/customerId}"
            items="{view>/customers}">
            <core:Item key="{view>ID}" text="{view>customerNumber} - {view>name}" />
          </Select>

          <Label text="Delivery Date" />
          <DatePicker
            value="{dialog>/deliveryDate}"
            valueFormat="yyyy-MM-dd"
            displayFormat="medium" />

          <Label text="Notes" />
          <TextArea
            value="{dialog>/notes}"
            rows="3"
            growing="true"
            maxLength="500" />

          <Label text="Currency" />
          <Select selectedKey="{dialog>/currency}">
            <core:Item key="USD" text="USD - US Dollar" />
            <core:Item key="EUR" text="EUR - Euro" />
            <core:Item key="GBP" text="GBP - British Pound" />
            <core:Item key="INR" text="INR - Indian Rupee" />
          </Select>
        </f:content>
      </f:SimpleForm>
    </content>
    <beginButton>
      <Button text="Create" type="Emphasized" press="onCreateOrderConfirm" />
    </beginButton>
    <endButton>
      <Button text="Cancel" press="onCreateOrderCancel" />
    </endButton>
  </Dialog>
</core:FragmentDefinition>
